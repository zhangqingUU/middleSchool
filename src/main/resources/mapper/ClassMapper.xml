<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.school.result.mapper.ClassMapper">
    <select id="setClaByGradeId" resultType="com.school.result.pojo.Class2" parameterType="int">
        select classId,className,gradeId from class where gradeId=#{gradeId}
    </select>
</mapper>