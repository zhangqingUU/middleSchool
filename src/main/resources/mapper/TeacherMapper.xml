<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.school.result.mapper.TeacherMapper">
    <select id="getInfo" parameterType="String" resultType="Teacher">
        select * from teacher where phone=#{phone} and loginpwd=#{loginPwd}
    </select>

</mapper>